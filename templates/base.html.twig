<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
        <link rel="stylesheet" href="{{ asset('/css/style.css') }}">

    </head>
    <body>
        <nav>
            <a href="{{ path('app_stagiaire') }}">Liste des stagiaires</a>
            <a href="{{ path('app_categorie') }}">Liste des catégories</a>
            <a href="{{ path('app_module') }}">Liste des modules</a>
            <a href="{{ path('app_session') }}">Liste des sessions</a>
        </nav>
        {% block body %}
        {% endblock %}
        {% block javascripts %}
            <script src="{{ asset('app/css/style.css') }}"></script>
        {% endblock %}
    </body>
</html>
